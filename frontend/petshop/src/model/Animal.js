class Animal {
  constructor(id, name, origin, description, image) {
    this.id = id;
    this.name = name;
    this.origin = origin;
    this.description = description;
    this.image = image;
  }
}

export default Animal;